import { init } from "./display";


init();




/* 
display most recent project name and description (first project in array)

new project button
    display modal form, create new project object on submit
    add to project array.

display project names in sidebar
    for each function that looks scans project arrays

display todos under project
    each todo has a check radio button and delete button

add todo button
    
today's tasks
    if any tasks have a due date of today, display the project name and the task
upcoming
    display all tasks that aren't due today
*/